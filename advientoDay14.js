// Switch statement
// When you can reduce, try ternary operator instead of switch

const day = 0

switch(day) {

    case 1: console.log("Monday"); break;
    case 2: console.log("Tuesday"); break;
    default: console.log("Not Found"); break;
}
